CREATE PROCEDURE USP_INSERT_PRODUCT
	@NAME VARCHAR(40),
	@DESCRIPTION VARCHAR(MAX),
	@PRICE DECIMAL
AS
	SET NOCOUNT ON;

	INSERT INTO [dbo].[Products]
	VALUES
	(
		@NAME,
		@DESCRIPTION,
		@PRICE
	);
